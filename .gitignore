# Rust build artifacts
/target/
Cargo.lock

# Local databases and media storage
clipboard.db
clipboard.db-shm
clipboard.db-wal
clip_images/

# Runtime logs and machine-specific configs
*.log
clipboard_daemon.log
device_config.json
peers.json

# Local AI models — large binary files, not source; download separately
# (see README for the exact ollama/huggingface source), don't commit them.
models/*.gguf

# Flutter / Android companion (added ahead of Step 5)
android_companion/build/
android_companion/.dart_tool/
android_companion/.flutter-plugins
android_companion/.flutter-plugins-dependencies
*.iml
.idea/